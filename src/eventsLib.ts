import { dispatchAction, registerEvents } from './lib/registerEvents';
import { EVENTS } from './lib/eventsList';

export { registerEvents, dispatchAction, EVENTS };

export default { registerEvents, dispatchAction, EVENTS };
